{
  "name": "markdown-plan-viewer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R public/* dist && browserify src/index.js -o dist/index.js",
    "start": "light-server -s dist -p 8000 -w \"src/**, public/** # npm run build\""
  },
  "author": "Rex Garland",
  "license": "ISC",
  "dependencies": {
    "cytoscape": "^3.20.0",
    "light-server": "^2.9.1"
  }
}
